#include <stdio.h>
/**
 * main - Print values of a from 0 to 9
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a = 0;
	while (a < 10; a++)
	{
		putchar (a);
	}
	putchar (\n);
	return (0);
}
