#include "main.h"
/**
 *mul - To show the multiplication of 2 numbers.
 *@a: first integer.
 *@b: sexond integer.
 *Return: Product of a and b.
 */
int mul(int a, int b)
{
        int product;

        product = a * b;
	return (product);
}
